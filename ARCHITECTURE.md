# logos-engine architecture
A real daemon and thin CLI. Signal → Nexus → SEMA Kameo actors fetch CoreLogos from central Sema and project it through the real TextualRust codec. The core item conversion is an rkyv round trip solely to reconcile the existing producer's abbreviated-but-pushed Git source identity with the contract's full exact revision; remove it when the producer manifest is normalized.
